name: Release

on:
  push:
    tags:
      - "*.*.*"
      - "*.*.*-*"

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.name }}

    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Linux x64
            os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            asset: linux-x64
            extension: ""

          - name: macOS x64
            os: macos-15-intel
            target: x86_64-apple-darwin
            asset: darwin-x64
            extension: ""

          - name: Windows x64
            os: windows-latest
            target: x86_64-pc-windows-msvc
            asset: windows-x64
            extension: ".exe"

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-${{ matrix.target }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-${{ matrix.target }}-cargo-

      - name: Build
        run: cargo build --locked --release --target ${{ matrix.target }}

      - name: Prepare release asset
        shell: bash
        run: |
          mv \
            "target/${{ matrix.target }}/release/prism-discord-rpc${{ matrix.extension }}" \
            "prism-rpc-v${{ github.ref_name }}-${{ matrix.asset }}${{ matrix.extension }}"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: prism-rpc-${{ matrix.asset }}
          path: prism-rpc-v${{ github.ref_name }}-${{ matrix.asset }}${{ matrix.extension }}

  release:
    name: Create GitHub Release
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download binaries
        uses: actions/download-artifact@v4
        with:
          path: release-assets
          merge-multiple: true

      - name: Extract release notes
        env:
          VERSION: ${{ github.ref_name }}
        run: |
          awk -v version="$VERSION" '
            $0 == "## " version {
              found = 1
              next
            }

            found && /^## / {
              exit
            }

            found {
              print
            }
          ' CHANGELOG.md > release-notes.md

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: "[**prism-discord-rpc v${{ github.ref_name }}**](https://github.com/Lunyyx/prism-discord-rpc/releases/tag/${{ github.ref_name }})"
          body_path: release-notes.md
          prerelease: ${{ contains(github.ref_name, '-') }}
          files: release-assets/*